function exponential(number) {
  return number* number;  
 
}
function isEven(number) {
  return number % 2 === 0;
}

for ( let number = 5 ; number < 20; number++ ){ 
     if (number % 2 ===0 ) {
    console.log("The exponential of " + number + " is " + number*number )}
  
    /* var message1 = number*number;
  var message2 = "The exponential of " + number + " is " ;
  console.log(message2,message1);  
  here it will show the odd and even numbers */ 
}

  
 